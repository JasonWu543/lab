# Phase 4.2 POSTMORTEM

> 完成实现和实验后填写。请用自己的运行数据支撑结论。

## 1. PD 分离改善了什么？

- 比较 PD 与合并部署的 TTFT、TPOT（至少报告 p50/p95）。
- 为什么合并部署中，长 prompt 的 prefill 会推高在座请求的 TPOT 尾部？
- 哪项指标一定改善、哪项取决于迁移成本？结合 tick 时间线解释。

## 2. Chunked prefill 与 PD 分离

- 两者分别隔离了哪一种资源争用或队首阻塞？
- 给出一个二者互补的 workload，以及一个可以互替的 workload。
- 改变 `chunk_size` 后，短请求 TTFT 与总体吞吐发生了什么？

## 3. KV transfer 盈亏平衡

定义你使用的合并部署等待成本、PD prefill 等待成本和迁移成本，写出“PD 的
TTFT 更低”所需的不等式。把 `transfer_ticks` 的固定项与带宽项代入，解出带宽
或 prompt 长度的临界条件，并用至少两组模拟参数验证不等式两侧的方向。

## 4. 复盘

- 哪个边界条件最容易产生 off-by-one？
- 当前“迁移无限并发”的简化会怎样偏置结论？
- 若加入有限迁移通道，你会新增哪些状态和验收测试？
