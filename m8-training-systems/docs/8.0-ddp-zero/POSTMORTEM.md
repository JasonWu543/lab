# Phase 8.0 Postmortem

> 完成实现并通过测试后填写。请用自己的实验数据回答，不要只复述课件。

## 梯度桶

- 为什么 DDP 通常按参数逆序分桶？这与 backward 的梯度就绪顺序有什么关系？
- bucket 太小与太大分别损失什么？记录一个你观察到的通信次数/耗时变化。

## ZeRO

- ZeRO-1、ZeRO-2、ZeRO-3 各分片什么状态？每一级引入或扩大了什么通信？
- 你的 `state_bytes()` 实测值与闭式账目是否一致？列出每个 rank 的结果。

## 等价性的边界

- 数据不等量、loss 使用 `sum`、通信或计算改用 bf16 时，梯度等价性分别怎样变化？
- `torch.equal` 与带容差对拍各自适合验证什么？记录本机整批与分批归约的最大绝对误差。

## 延伸实验

- 真实多卡下补测带宽、bucket size 与加速比，并把环境和结果加入 BASELINE/backlog。
